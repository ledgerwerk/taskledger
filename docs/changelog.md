# Changelog

> **Generated by releaseledger.** Manual edits may be overwritten. Update releaseledger entries and rebuild this file instead.

## [v0.6.5] - 2026-08-22

### Added

- Added run-scoped read-only review handoffs with cross-harness claims, durable evidence, and snapshot-drift detection

### Changed

- Changed workspace snapshot validation to track added and deleted project files and reject unstable snapshot baselines

### Quality

- Improved regression coverage for review-handoff identity, claims, command registration, and service boundaries

<!-- generated by releaseledger -->

## [v0.6.4] - 2026-08-21

### Added

- Added separate runtime and log storage mounts and closed uninitialized projects against legacy storage creation

### Changed

- Improved managed command virtualenv selection for planning and implementation

### Documentation

- Documented the architecture source and generated output under docs/architecture.md

<!-- generated by releaseledger -->

## [v0.6.3] - 2026-08-19

### Changed

- Improved managed commands to preserve child arguments, working directories, output, and execution context

<!-- generated by releaseledger -->

## [v0.6.2] - 2026-08-15

### Changed

- Automatic indexes cache bootstrapping before canonical mutations with quarantine-based recovery for unbound caches
- Indexes mount is fixed to cache storage; `storage set indexes cache` is now an idempotent topology request and non-cache indexes storage is rejected directly

### Fixed

- `reindex` command now reports cache recovery results and surfaces structured errors for recovery failures

### Documentation

- Documented indexes cache recovery behavior and multi-machine sync workflow

<!-- generated by releaseledger -->

## [v0.6.1] - 2026-08-14

### Added

- Atomic index update functions for dependency and introduction indexes
- Ledgercore version inspection with module and distribution provenance tracking

### Changed

- Ledgercore minimum version requirement updated to >=0.6.1

### Fixed

- Workspace snapshot hash exclusions now resolve Taskledger state roots relative to the Git root

### Quality

- Test suite performance improvements with atomic fast I/O and focused fixtures

<!-- generated by releaseledger -->

## [v0.6.0] - 2026-07-28

### Added

- Ledgercore 0.6.0 compatibility boundary with strict public-API imports and version validation
- Unified CLI envelope format with `ledgerwerk.cli.v1` schema and space-separated command paths
- Family exit code mapping (0-5 only): SUCCESS, DOMAIN_FAILURE, USAGE, UNAVAILABLE, CONFLICT, EXTERNAL_FAILURE
- `--root` as canonical global option; `--cwd` is now a hidden deprecated alias
- `taskledger X` canonical version output format
- Nested `help` command with CommandInventory integration
- `info` command replacing deprecated `status --full` usage
- Tolerant root callback for init/status/info/doctor/commands/help/storage/migrate
- Command metadata family schema mapping with Taskledger extensions
- `config validate` and `config unset` commands
- `storage validate --strict` with additional binding/fingerprint checks
- Canonical storage options: `--storage-root`, `--scope`
- `migrate recover` command with `--journal`, `--policy`, `--dry-run` options
- `migrate cleanup` command with `--journal`, `--dry-run`, `--yes` options
- Taskledger migration hooks: quiescence_check, validate_staged, validate_activated, finalize
- Ledgercore 0.3 canonical `.ledger/` layout support with split data, logs, and indexes mounts, explicit legacy migration, and compatibility routing

### Changed

- Updated Ledgercore dependency to `>=0.5.0,<0.7.0` (supports 0.6.0)
- Updated exit codes to family contract: NOT_FOUND 5->3, STORAGE 6->4, VALIDATION 7->1
- `storage set` is now topology-only (no data movement)
- Command paths now use space separators instead of dots

### Deprecated

- `--cwd` global option (use `--root` instead)
- `status --full` (use `info` instead)
- Storage options `--root`, `--project`, `--local` in `storage set`

<!-- generated by releaseledger -->

<!-- releaseledger:generated-file -->
